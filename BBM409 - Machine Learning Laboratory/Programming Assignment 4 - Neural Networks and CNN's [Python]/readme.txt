Deficiencies (if exists):
- (For standart neural network) vanishing gradient problem (In other words if dataset includes so many features, neural network doens't learn. Loss function decrease but accuracy don't increase.)
